services:
  backend:
    build: ./backend
    container_name: app-template-backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development

  frontend:
    build: ./frontend
    container_name: app-template-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend