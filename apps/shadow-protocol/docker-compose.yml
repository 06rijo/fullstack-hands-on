services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - shadow-data:/data
    environment:
      - DB_PATH=/data/shadow.db

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  shadow-data:
